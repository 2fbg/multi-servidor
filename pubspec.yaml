name: multi_servidor
description: App de streaming multi-servidor
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  adaptive_theme: ^3.7.2
  video_player: ^2.8.6
  chewie: ^1.7.5
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2
  http: ^1.1.0
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
